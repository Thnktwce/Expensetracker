<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Views.MainPage">
    <StackLayout Padding="10">
        <Button Text="Добавить расход" Clicked="OnAddExpenseClicked" />

        <!-- Лейбл для отображения суммы расходов -->
        <Label x:Name="TotalAmountLabel" FontSize="Large" HorizontalOptions="Center" />

        <CollectionView x:Name="ExpensesCollection">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="5">
                        <Label Text="{Binding Title}" FontAttributes="Bold" />
                        <Label Text="{Binding Amount}" />
                        <Label Text="{Binding Comment}" FontAttributes="Italic" />
                        <Label Text="{Binding Date}" />
                        <Button Text="Редактировать" Clicked="OnEditExpenseClicked" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>
